apiVersion: apps/v1
kind: Deployment
metadata:
  name: posts-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: posts-pod
  template:
    metadata:
      labels:
        app: posts-pod
    spec:
      containers:
        - name: posts-server
          image: gcr.io/gks-cluster-316500/posts
          imagePullPolicy: Always
          ports:
            - name: posts
              containerPort: 4242
