syntax = "proto3";

service RouteGuide {
    rpc book(Message) returns (Message) {}
//    rpc showbooking(Message) returns (Message) {}
}

message Message { //Information that will be passed between client and service
                  string data = 1;
//                  string text = 2;
}