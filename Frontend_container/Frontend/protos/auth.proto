syntax = "proto3";

service AuthService {
  rpc Login(Credentials) returns (UserId) {}
  rpc Signup(Credentials) returns (SignupResult) {}
}

message Credentials {
  string email = 1;
  string password = 2;
}

message UserId { 
  string id = 1;
}

message SignupResult {
  string password = 1;
}